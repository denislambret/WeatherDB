# Get min, max, averages for curdate()
#------------------------------------------------------------------------------
call Get_Today_Avg(curdate(),6);
call Get_Today_Min(curdate(),6);
call Get_Today_Max(curdate(),6);